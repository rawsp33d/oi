Dog :: struct {
	name: string
	greeting : string = "woof"
	# TODO: get this working
	# greeting := "woof"
}

Dog :< {
	greet :: fn(self) string {
		"{self.name} says {self.greeting}!"
	}
}

dog := Dog.{
	name = "Larry"
	greeting = "bark"
}
dog.greet()
