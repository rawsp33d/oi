Dog :: struct {
	name: string
	greeting : string = "woof"
}

Dog :{
	greet :: fn(self) string {
		"{self.name} says {self.greeting}!"
	}
}

dog := Dog.{
	name = "Larry"
	greeting = "bark"
}
dog.greet()
